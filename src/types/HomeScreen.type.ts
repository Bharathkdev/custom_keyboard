import {ImageStyle, TextStyle, ViewStyle} from 'react-native';

export type StylePropTypes = {
  container: ViewStyle;
  titleBarContainer: ViewStyle;
  titleBarImage: ImageStyle;
  titleBarText: TextStyle;
};
